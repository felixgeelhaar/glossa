apiVersion: rollops.klarlabs.de/v1
kind: RolloutConfig
metadata: { name: gl-svc-api }
spec:
  target:
    kind: kubernetes
    ref: glossa/prod/svc-api
    criticality: medium
    spec:
      namespace: glossa
      resource: service/api
      manifest: |
        apiVersion: v1
        kind: Service
        metadata:
          annotations:
            meta.helm.sh/release-name: glossa
            meta.helm.sh/release-namespace: glossa
            obvia.io/depends-on: postgres
          labels:
            app.kubernetes.io/instance: glossa
            app.kubernetes.io/managed-by: Helm
            app.kubernetes.io/name: glossa
            app.kubernetes.io/version: 0.1.0
            helm.sh/chart: glossa-0.1.1
          name: api
          namespace: glossa
        spec:
          internalTrafficPolicy: Cluster
          ports:
          - port: 8080
            protocol: TCP
            targetPort: 8080
          selector:
            app: api
          sessionAffinity: None
          type: ClusterIP
  strategy: { type: rolling }
