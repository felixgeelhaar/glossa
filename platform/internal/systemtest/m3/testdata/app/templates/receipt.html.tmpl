{{/* Written by `go generate ./internal/systemtest/m3/...` — change the generator, not this file. */}}
{{define "receipt"}}<!doctype html>
<html lang="{{lang}}" dir="{{dir}}">
  <body>
    <p>{{t "layout.croissant.title"}}</p>
    <p>{{t "layout.basket.label"}}</p>
    <p>{{t "layout.plait.title"}}</p>
    <p>{{t "invoice.flour.label"}}</p>
    <p>{{t "invoice.branch.title"}}</p>
    <p>{{t "invoice.crust.count"}}</p>
    <p>{{t "invoice.oven.activity"}}</p>
    <p>{{t "invoice.salt.title"}}</p>
  </body>
</html>{{end}}
