// Code generated by glossa generate from locales/en.json. DO NOT EDIT.

// Package msg has typed accessors for the project's messages.
package msg

import (
	"context"
	"time"

	glossa "github.com/felixgeelhaar/glossa/runtimes/go"
)

// Localizer renders a message by ID; *glossa.Localizer implements it.
type Localizer interface {
	T(id string, args glossa.Args, opts ...glossa.Option) string
}

// Contextual finds the locale on a request's context; *glossa.Client
// implements it.
type Contextual interface {
	Localizer(ctx context.Context) *glossa.Localizer
}

// Messages are the project's messages with typed arguments:
//
//	m := For(client.For(user.Locale))
//	subject := m.CheckoutPay(order.Total)
type Messages struct{ l Localizer }

// For binds the accessors to a localizer.
func For(l Localizer) Messages { return Messages{l: l} }

// FromContext binds the accessors to the locale on ctx (set by
// glossa's middleware).
func FromContext(ctx context.Context, c Contextual) Messages { return For(c.Localizer(ctx)) }

// AthleteGreeting renders athlete.greeting: {gender, select, female {Hi {name}} other {Hello {name}}}
func (m Messages) AthleteGreeting(gender string, name string, opts ...glossa.Option) string {
	return m.l.T("athlete.greeting", glossa.Args{"gender": gender, "name": name}, opts...)
}

// CartCheckout renders cart.checkout: Zur Kasse
func (m Messages) CartCheckout(opts ...glossa.Option) string {
	return m.l.T("cart.checkout", nil, opts...)
}

// CartItems renders cart.items: {count, plural, one {# item} other {# items}}
func (m Messages) CartItems(count float64, opts ...glossa.Option) string {
	return m.l.T("cart.items", glossa.Args{"count": count}, opts...)
}

// CheckoutPay renders checkout.pay: Pay {amount, number, ::currency/EUR}
func (m Messages) CheckoutPay(amount float64, opts ...glossa.Option) string {
	return m.l.T("checkout.pay", glossa.Args{"amount": amount}, opts...)
}

// CheckoutPaymentFailed renders checkout.payment-failed: Payment failed: {reason}
func (m Messages) CheckoutPaymentFailed(reason string, opts ...glossa.Option) string {
	return m.l.T("checkout.payment-failed", glossa.Args{"reason": reason}, opts...)
}

// LegalType renders legal.type: {type} *\/ with a comment breaker
func (m Messages) LegalType(typeArg string, opts ...glossa.Option) string {
	return m.l.T("legal.type", glossa.Args{"type": typeArg}, opts...)
}

// NavHome renders nav.home: Home
func (m Messages) NavHome(opts ...glossa.Option) string {
	return m.l.T("nav.home", nil, opts...)
}

// NavHomeTitle renders nav.home.title: Home title
func (m Messages) NavHomeTitle(opts ...glossa.Option) string {
	return m.l.T("nav.home.title", nil, opts...)
}

// OrderShipped renders order.shipped: Shipped on {date, date, short} at {time, time, short}
func (m Messages) OrderShipped(date time.Time, timeArg time.Time, opts ...glossa.Option) string {
	return m.l.T("order.shipped", glossa.Args{"date": date, "time": timeArg}, opts...)
}

// IDs are the message IDs, for dynamic use (glossa.Localizer.T).
const (
	IDAthleteGreeting       = "athlete.greeting"
	IDCartCheckout          = "cart.checkout"
	IDCartItems             = "cart.items"
	IDCheckoutPay           = "checkout.pay"
	IDCheckoutPaymentFailed = "checkout.payment-failed"
	IDLegalType             = "legal.type"
	IDNavHome               = "nav.home"
	IDNavHomeTitle          = "nav.home.title"
	IDOrderShipped          = "order.shipped"
)
