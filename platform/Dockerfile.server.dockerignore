# Build context is the repository root (see Dockerfile.server). Send only
# the three Go modules the server is built from; tests and fixtures never
# reach the image.
*
!messageformat/
!runtimes/go/
!platform/
messageformat/js/
messageformat/testdata/
**/testdata/
**/*_test.go
**/node_modules/
**/.DS_Store
