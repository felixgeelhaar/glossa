{
  "name": "@felixgeelhaar/glossa-admin",
  "version": "0.0.0",
  "description": "Glossa admin SPA — Lit + Vite. Dogfoods @felixgeelhaar/glossa-elements and @felixgeelhaar/glossa-sdk.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@felixgeelhaar/glossa-elements": "workspace:*",
    "@felixgeelhaar/glossa-format": "workspace:*",
    "@felixgeelhaar/glossa-sdk": "workspace:*",
    "@felixgeelhaar/glossa-ui": "workspace:*",
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "jsdom": "^25.0.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
