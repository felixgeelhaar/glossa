// Bootstrap. Side-effect imports register every custom element
// the page references — there's no manual mounting after this.

import "@glossa/elements";

import "./admin-app.js";
import "./key-list.js";
import "./key-edit.js";
import "./demo-strip.js";
