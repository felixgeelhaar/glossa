apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: glossa
resources:
  - postgres.yaml
  - api.yaml
  - admin.yaml
  - ingress.yaml
  - backup-cronjob.yaml
# secrets.example.yaml is intentionally NOT listed — secrets are
# applied out of band so example values never ship to prod.
