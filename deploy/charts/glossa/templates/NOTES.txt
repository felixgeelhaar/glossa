Glossa {{ .Chart.AppVersion }} installed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

{{- if .Values.ingress.enabled }}

Reachable at:
{{- range .Values.ingress.hosts }}
  https://{{ .host }}
{{- end }}

DNS: point each host above (A or AAAA) at the public IP of your
ingress controller. Cert-manager picks up the Certificate resource
and issues TLS in 30–90 seconds.
{{- end }}

{{- if .Values.secrets.bootstrap.tenantSlug }}

First-run admin: log in with
  email:    {{ .Values.secrets.bootstrap.adminEmail }}
  password: (the value you set in secrets.bootstrap.adminPassword)
on the {{ .Values.secrets.bootstrap.tenantSlug }} tenant.

Rotate the bootstrap password right after the first login.
{{- end }}

To watch the rollout:
  kubectl -n {{ .Release.Namespace }} rollout status deploy/api
  kubectl -n {{ .Release.Namespace }} rollout status deploy/admin

To upgrade to a new image tag:
  helm upgrade {{ .Release.Name }} oci://ghcr.io/felixgeelhaar/charts/glossa \
    --version <chart-version> -n {{ .Release.Namespace }} \
    --set api.image.tag=v0.1.x --set admin.image.tag=v0.1.x
